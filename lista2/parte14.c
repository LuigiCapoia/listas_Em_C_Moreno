#include <stdio.h>
#include <stdlib.h>
#include <locale.h>
int main()
{
    // ex15
    int x;
    for (x = 15; x <= 30; x++)
    {
        printf("%i\n", x);
    }

    printf("\n");
    system("PAUSE");
    return 0;
}